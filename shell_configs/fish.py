"""Fish shell configuration handler — placeholder for future fish support."""

# Not implemented in v1.
# Shell config path: ~/.config/fish/config.fish
# Fish uses different syntax:
#   set -gx PATH <target>/bin $PATH
#   set -gx PYTHONPATH <target> $PYTHONPATH

SHELL_NAME = "fish"
DEFAULT_CONFIG_PATH = "~/.config/fish/config.fish"
