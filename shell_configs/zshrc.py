"""Zsh shell configuration handler — placeholder for future macOS/zsh support."""

# Not implemented in v1.
# Shell config path: ~/.zshrc
# Same marker format as bash.

SHELL_NAME = "zsh"
DEFAULT_CONFIG_PATH = "~/.zshrc"
